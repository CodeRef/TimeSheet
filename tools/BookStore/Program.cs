namespace BookStore
{
    public class Program
    {
        private static Root root;

        private static void Main(string[] args)
        {
            root = new Root();
            //var allBooks = root.Book.GetAll();
            //var allShop = root.Shop.GetAll();
            //var todo = root.ToDo.GetAll();
        }
    }
}